// This file sets scss variables for bootstrap, perform, and any other scss.
// It's important to import bootstrap variables in the correct place, see below.

// Set some variables before importing bootstrap.
// Disabling rounded corners, for example:
// $border-radius: 0;
// $border-radius-lg: 0;
// $border-radius-sm: 0;
// $badge-border-radius: .5em;
// This needs to be done before importing because bootstrap
// will use these values in many of its other variables.

// To customize bootstrap colors:
// $primary: red;
// $body-color: #13123a;

// Here is good place to override bootstrap default fonts and import any external fonts.
// @import url('https://fonts.googleapis.com/css?family=Arimo|Abel');
// $font-family-base: 'Arimo', Helvetica, Arial, Lucida, sans-serif;
// $headings-font-family: 'Abel', Helvetica, Arial, Lucida, sans-serif;

// Now import bootstrap variables, making them available when defining other variables.
@import "~bootstrap/scss/variables";

// Set variables that depend on imported bootstrap variables.
// $some-bg: $white;
// $some-var: theme-color('primary');

// Set some perform variables.
// $p-sidebar-bg: #4578a4;
// $p-sidebar-highlight: $yellow;

// Here's a common case of overriding the header color.
// Note that the header uses the 'navbar-light' bootstrap class for text colors.
// $p-header-bg: darken($orange, 10);
// $navbar-light-color: rgba($white,.8);
// $navbar-light-hover-color: rgba($white, 1);
// $navbar-light-active-color: rgba($white, 1);
// $navbar-light-disabled-color: rgba($white,.3);
// $navbar-light-toggler-border-color: rgba($white,.1);
