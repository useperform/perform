{% extends '@PerformBase/crud/edit.html.twig' %}

{% block workspace %}
  <h3><i class="fa fa-angle-right"></i> Edit playlist</h3>
  {{form_start(form)}}
  <div class="card">
    <div class="card-header">
      <h4>Details</h4>
    </div>
    <div class="card-body">
      {{perform_crud_edit_context(entity, 'title', form.vars.fields.title, form)}}
    </div>
  </div>

  <div class="card">
    <div class="card-header">
      <h4>Tracks</h4>
    </div>
    <div class="card-body">
      {{perform_crud_edit_context(entity, 'items', form.vars.fields.items, form)}}
    </div>
  </div>

  <button type="submit" class="btn btn-primary">Save</button>
  <a href="{{perform_crud_route(crudName, 'list')}}" class="btn btn-light" tabindex="-1">Cancel</a>
  {{form_end(form)}}
{% endblock %}

{% block js %}
  {{parent()}}
  <script type="text/javascript" src="{{asset('/bundles/performmediaplayer/js/admin.js')}}"></script>
{% endblock %}
